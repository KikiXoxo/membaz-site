[[headers]]
  # Match all files in the img directory and its subdirectories
  for = "/img/**/*"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"
    Pragma = "no-cache"
    Expires = "0"
